# ==================== NEXUS AIOPS DEPENDENCIES ====================

# Build Requirements (CRITICAL for Python 3.14+)
setuptools>=65.0.0
wheel>=0.41.0

# Core Web Framework
Flask>=3.0.0
Flask-CORS>=4.0.0
Flask-SocketIO>=5.3.0
python-socketio>=5.9.0
python-engineio>=4.7.0
Werkzeug>=3.0.0
Flasgger>=0.9.5  # Swagger/OpenAPI documentation

# Authentication & Security
PyJWT>=2.8.0
bcrypt>=4.0.0

# Database
pymongo>=4.5.0

# Environment Configuration
python-dotenv>=1.0.0

# Utilities
requests>=2.31.0
psutil>=5.9.0  # System metrics collection (CPU, memory, disk, network)
prometheus-client>=0.17.0  # Metrics export

# Data Science & ML (for Chaos Simulator & ML Pipeline)
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0

# PDF Generation (for Machine Analyzer PDF export)
reportlab>=4.0.0
Pillow>=10.0.0  # Used directly for image sizing when embedding PNGs into the PDF appendix

# Configuration
pyyaml>=6.0.0

# Additional utilities
joblib>=1.3.0

# Production WSGI Server & WebSocket
gunicorn>=21.0.0
eventlet>=0.33.0  # WebSocket support for Flask-SocketIO

# API Documentation
apispec>=6.0.0
marshmallow>=3.19.0

# Logging & Monitoring
python-json-logger>=2.0.0